[build]
builder = "nixpacks"
buildCommand = "npm ci && npx prisma generate && npm run build"

[deploy]
startCommand = "node_modules/.bin/prisma migrate deploy; echo \"Migration exit: $?\"; node dist/main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
